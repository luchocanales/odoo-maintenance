<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Form: insert fields below maintenance_team_id and hide user_id -->
    <record id="maintenance_request_form_customize" model="ir.ui.view">
        <field name="name">maintenance.request.form.customize</field>
        <field name="model">maintenance.request</field>
        <field name="inherit_id" ref="maintenance.hr_equipment_request_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='maintenance_team_id']" position="after">
                <field name="responsible_employee_id"/>
                <field name="workshop_partner_id"/>
                <field name="city_id" domain="[('country_id.code','=','PE')]" options="{'no_create': True}"/>
                <field name="delivery_guide"/>
            </xpath>

            <xpath expr="//field[@name='user_id']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
        </field>
    </record>

    <!-- Tree: replace user_id by responsible_employee_id -->
    <record id="maintenance_request_tree_customize" model="ir.ui.view">
        <field name="name">maintenance.request.tree.customize</field>
        <field name="model">maintenance.request</field>
        <field name="inherit_id" ref="maintenance.hr_equipment_request_view_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='user_id']" position="replace">
                <field name="responsible_employee_id"/>
            </xpath>
        </field>
    </record>

    <!-- Kanban: add field + show responsible below employee_id (fallback: below Requested by) -->
    <record id="maintenance_request_kanban_customize" model="ir.ui.view">
        <field name="name">maintenance.request.kanban.customize</field>
        <field name="model">maintenance.request</field>
        <field name="inherit_id" ref="maintenance.hr_equipment_request_view_kanban"/>
        <field name="arch" type="xml">
            <xpath expr="//kanban/field[@name='user_id']" position="after">
                <field name="responsible_employee_id"/>
            </xpath>

            <!-- Preferred: below employee_id if it exists in the template -->
            <xpath expr="//t[@t-name='kanban-box']//field[@name='employee_id']" position="after" optional="1">
                <span name="responsible_employee_id" t-if="record.responsible_employee_id.raw_value" class="text-success">
                    Responsable: <field name="responsible_employee_id"/><br/>
                </span>
            </xpath>

            <!-- Fallback: below 'Requested by' block in standard view -->
            <xpath expr="//t[@t-name='kanban-box']//span[@name='owner_user_id']" position="after" optional="1">
                <span name="responsible_employee_id" t-if="record.responsible_employee_id.raw_value" class="text-success">
                    Responsable: <field name="responsible_employee_id"/><br/>
                </span>
            </xpath>
        </field>
    </record>

</odoo>
